[theme]
base = "dark"
primaryColor = "#9F8BFF"
backgroundColor = "#0D0A1C"
secondaryBackgroundColor = "#171232"
textColor = "#F4F1E8"

[server]
# Silence the dev file-watcher (avoids noisy torch/module reload logs).
fileWatcherType = "none"
headless = true
