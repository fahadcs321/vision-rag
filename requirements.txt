# Vision RAG — base runtime (slim).
#
# This is the lightweight stack: the API skeleton, the Streamlit UI (incl. the
# hosted DEMO_MODE), PDF rendering, Qdrant, and the offline tests. It installs
# cleanly on Streamlit Cloud and in CI with no GPU.
#
# The heavy visual-model stack (ColQwen2.5, Qwen2-VL, torch) lives in
# requirements-models.txt — install it for real indexing/answering:
#     pip install -r requirements.txt -r requirements-models.txt

# ── Vector database ──────────────────────────────────────────────────────────
qdrant-client>=1.10.0

# ── PDF rendering + images ───────────────────────────────────────────────────
PyMuPDF>=1.24.0                  # fitz — fast renderer, handles scanned PDFs
Pillow>=10.4.0

# ── API ──────────────────────────────────────────────────────────────────────
fastapi>=0.115.0
uvicorn[standard]>=0.30.0
python-multipart>=0.0.9          # file upload support
pydantic>=2.7.0
aiofiles>=23.2.0

# ── Hosted vision answerer (OpenAI-compatible; Groq by default) ──────────────
openai>=1.40.0

# ── UI ───────────────────────────────────────────────────────────────────────
streamlit>=1.35.0

# ── Utilities ────────────────────────────────────────────────────────────────
numpy>=1.26.0
requests>=2.32.0
python-dotenv>=1.0.0
rich>=13.7.0
tqdm>=4.66.0
