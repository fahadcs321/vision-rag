# Dev / CI tooling — light, offline. The offline test suite fakes the heavy model
# and Qdrant, so it needs none of the GPU/ML stack from requirements.txt.
ruff>=0.5.0
pytest>=8.2.0
numpy>=1.26.0
Pillow>=10.4.0
PyMuPDF>=1.24.0
qdrant-client>=1.10.0
